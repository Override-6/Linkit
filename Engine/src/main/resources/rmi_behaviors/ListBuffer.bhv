describe class scala.collection.mutable.ListBuffer {
    forall field -> disable
    forall method -> enable as broadcast

    enable addOne as broadcast {
        args {
            elem -> enable
        }
        returnvalue -> disable
    }

    enable remove as addOne

    disable map
    disable filter
}