package fr.linkit.engine.internal.mapping

import java.net.URL
import scala.collection.mutable.ListBuffer

object ResourceMappings {

    private val resources = ListBuffer.empty[URL]

    def addResource(root: URL): Unit = {

    }

}
