package fr.linkit.engine.local.language.bhv.parsed

import fr.linkit.api.connection.cache.obj.behavior.SystemBehavior
import fr.linkit.api.local.language.bhv.context.{BehaviorStoreContext, ParsedBehavior}

class ParsedClassBehavior() extends ParsedBehavior {
    override def getName: String =

    override def toSystemBehavior(context: BehaviorStoreContext): SystemBehavior = {
        
    }
}
